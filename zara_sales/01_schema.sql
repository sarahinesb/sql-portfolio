-- Schema for zara_sales

CREATE TABLE product_dataset (
    `Product ID` INT NULL,
    `Product Position` TEXT NULL,
    `Promotion` TEXT NULL,
    `Product Category` TEXT NULL,
    `Seasonal` TEXT NULL,
    `Sales Volume` INT NULL,
    `brand` TEXT NULL,
    `url` TEXT NULL,
    `sku` TEXT NULL,
    `name` TEXT NULL,
    `description` TEXT NULL,
    `price` DOUBLE NULL,
    `currency` TEXT NULL,
    `scraped_at` TEXT NULL,
    `terms` TEXT NULL,
    `section` TEXT NULL
);
